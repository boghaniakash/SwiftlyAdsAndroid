<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose supplies its own Material theme; the Activity only needs a valid base theme. -->
    <style name="Theme.SwiftlyAdsSample" parent="@android:style/Theme.Material.Light.NoActionBar" />
</resources>
