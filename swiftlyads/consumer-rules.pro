# SwiftlyAds keeps no reflective public API surface beyond what AdMob/UMP need.
# The Google Mobile Ads SDK ships its own consumer ProGuard rules, so nothing
# extra is required here. Left intentionally minimal.
